apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-pg-logs
  namespace: storedog
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
